
let bonus = 20 ;

function sum ( first , second)
{
    let result = first + second + bonus ;
    
    if( result > 9)
    {
        var mood = " happy";
        mood = " Cranky"
        // console.log( mood);
    }

    // console.log(bonus);
    
    var day = " friday";
    console.log ( day);

    return result ;
}

const output = sum( 3 , 7);
// console.log( output);